# 生产站管理员（智能体交单用）

线上：https://zx.xingmaierp.cc

## 登录

- 用户名：`罗成` 或 `luocheng`
- 密码：`jingdong220`
- 登录接口：`POST /api/auth/login`，JSON `{ "username", "password" }`

不要再用演示账号。`ChangeMe123!` 登不上生产站，交单会失败。

## 交单

- 申请人、来源一律写：`罗成运营部主脑`
- 版本号向 `GET /api/releases/next` 领取，不要自编占用
- 闸门只点队首；交单可带 `contents`（路径 → 正文）
- 允许模块以闸门当时列表为准；公告中心页面改动常挂 **主框架**，组织中心挂 **组织中心**

## 禁止

- 不要把生产密码写进单据标题、摘要、验收、登录页、公开前端文案
- 不要把生产密码当成成员「初始密码」或写进花名册默认值
- 不要 SSH / `systemctl`；不要改花名册管辖源
