{
  "name": "mengkai",
  "version": "0.1.0",
  "description": "Mengkai starter web application (notes API + frontend).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2",
    "mysql2": "^3.24.3"
  }
}
