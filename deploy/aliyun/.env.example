# 阿里云 ECS 连接配置（复制为 deploy/aliyun/.env 后填写，勿提交到 Git）
ALIYUN_HOST=8.140.33.133
ALIYUN_USER=root
ALIYUN_PORT=22
ALIYUN_APP_DIR=/opt/yunyingbu
ALIYUN_DEPLOY_BRANCH=main

# 可选：应用运行时环境变量（部署时会写入服务器 .env）
# APP_PORT=8080
